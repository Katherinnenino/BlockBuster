<p style="color: green"><%= notice %></p>

<%= render @movie %>

<div>
  <%= link_to "Editar", edit_movie_path(@movie) %> |
  <%= link_to "Regresar", movies_path %>

  <%= button_to "Borrar", @movie, method: :delete %>
</div>
